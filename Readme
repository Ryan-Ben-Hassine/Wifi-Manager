Requirements
	•	Python 3.8+
	•	Scapy (pip install scapy)
	•	Linux/macOS recommended (uses fcntl, struct).

___________________________

python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install scapy
